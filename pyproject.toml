[project]
name = "hex-game"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pygame>=2.6.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
